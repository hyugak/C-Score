ーーーサーバ処理ーーー
[mulka2]
xmlデータ吐き出し
↓
XSLT変換
↓
[xml/datapost.bat]
データ送信
↓
[php/xml_receive.php]
データ受け取り
↓
XMLオブジェクトを配列化
↓
クラスごとに配列化
↓
[php/rank_input.php]
順位ごとの点数配列を０から順番にとり、参加点と合算し、チーム名＆点数の配列を作る
クラスごとの配列を順位昇順にする
↓
DB<point>から各順位・クラス・ポイントを引き出す
↓
クラスごとのループ＆順位ごとのループでチームごとに順位点と参加点を合算
↓
合算した最終的な点数とチーム名を一つの配列にする
↓
配列を点数昇順
↓
ループで10位まで絞る
↓
配列をDB登録

ーーースコア表示ーーー
[index.html]
Ajax通信で[php/db_output]に要求
↓
[php/db_output.php]
DBコネクト(外部ファイル[php/db_connect.php]をrequire_once()で組み込み)
↓
DBから点数昇順で取り出す
↓
変数にHTMLタグと取り出したデータをループで書き込み
↓
print でHTML表示
↓
[index.html]
スコア表示